# !/bin/bash
# ~/.tmuxinator/learn-layout.yml

name: learn-layout
root: /Users/jose8a/jdev/l2/learn-layout

windows:
  - editors:
      layout: even-horizontal
      panes:
        - chrome index.html && vim index.html
        - vim main.css
  - generic:
      layout: even-horizontal
      panes:
        - chrome .resources.md && vim .resources.md
        - txd
